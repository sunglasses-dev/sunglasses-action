# Copy this file to .github/workflows/sunglasses.yml in your repo.
name: Sunglasses — agent file scan
on: [pull_request]

permissions:
  contents: read
  security-events: write   # findings appear in the PR "Code scanning" tab

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: sunglasses-dev/sunglasses-action@v1
        # with:
        #   paths: "README.md docs/ prompts/ .claude/"   # directories are walked
        #   fail-on-threat: true
        #   fail-on-incomplete: true   # fail when a file could not be read at all
        #   upload-sarif: true
        #   version: "==0.5.6"         # empty installs the latest release at run time
